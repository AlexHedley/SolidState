@{
    var commentEngine = Document.GetString("CommentEngine", string.Empty).ToLowerInvariant();
}
@if (!string.IsNullOrEmpty(commentEngine) && commentEngine != "none")
{
    <div id="comments-container">
        @Html.Partial("_post-comments-" + commentEngine)
    </div>
}
